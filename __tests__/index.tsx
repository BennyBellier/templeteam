// Just a simple test to verify that the test suite is working
// and that the test environment is set up correctly
describe('Test Suite', () => {
  it('should pass', () => {
    expect(true).equal(true);
  });
});